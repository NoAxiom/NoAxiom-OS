[package]
name = "ksync"
version = "0.1.0"
edition = "2021"

[dependencies]

# Local crates
arch = {path = "../arch"}
config  = {path = "../config"}
# External crates
kernel-sync = { git = "https://github.com/os-module/kernel-sync.git" }
riscv = "0.12"
spin = "0.9"
log = "0.4"